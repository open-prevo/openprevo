package org.example.prevo.open.adapter.repository;

import org.example.prevo.open.adapter.dto.JobEndDTO;
import org.springframework.data.jpa.repository.JpaRepository;

public interface JobEndRepository extends JpaRepository<JobEndDTO, Long> {
}
